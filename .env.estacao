# Configuração para Estação da Empresa
# Backend API local (ajustar IP conforme necessário)
VITE_API_BASE_URL=http://localhost:8001/api/v1

# Supabase desabilitado - valores placeholder para evitar erros de import
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder-key-not-used

# Notas:
# - Login/Autenticação: Backend API ✅
# - Upload de documentos: MinIO no Backend ✅
# - Ajustar VITE_API_BASE_URL para o IP/porta correto do backend na estação
